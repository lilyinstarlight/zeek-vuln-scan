[package]
description = Vulnerability scanning TCP detection
test_command = (cd tests && btest -d)
script_dir = scripts
tags = bro plugin, scan detection
depends =
  bro >=2.5.1
  bro/ncsa/bro-is-darknet >=0.3
